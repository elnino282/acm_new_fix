# --- Dependency Management ---
# QUAN TRỌNG: Tự động ghim chính xác version (bỏ dấu ^ hoặc ~) khi chạy npm install.
# Giúp package-lock.json ổn định tuyệt đối, tránh lỗi "works on my machine".
save-exact=true

# Đảm bảo tạo ra package-lock.json khi cài đặt
package-lock=true

# --- Environment & Security ---
# Bắt buộc tuân thủ phiên bản Node/npm khai báo trong package.json "engines"
# Ngăn chặn việc dev dùng sai version Node gây lệch lockfile.
engine-strict=true

# --- Performance & UX (Giảm nhiễu) ---
# Tắt thông báo "funding" (donate) để log sạch hơn
fund=false

# Tắt audit tự động khi install để tăng tốc độ (nên chạy audit ở pipeline riêng)
audit=false

# Chỉ hiện warning và error, giảm bớt log info không cần thiết
loglevel=warn

# --- Network/Timeout (Tùy chọn cho mạng chậm) ---
# Tăng timeout nếu project quá lớn hoặc mạng không ổn định (mặc định là 300000)
# fetch-retry-mintimeout=20000
# fetch-retry-maxtimeout=120000